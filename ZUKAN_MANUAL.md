
# 野鳥図鑑・更新マニュアル

このマニュアルは、野鳥図鑑のコンテンツを管理するための手順書です。
プログラミングの知識は不要で、いくつかのファイルを編集するだけで図鑑を更新できます。

## 重要なファイルとフォルダ

あなたが主に触る必要があるのは、以下の2つ（＋ブログ化したい場合は3つ）だけです。

1.  **`/src/data/birds.json`**: 
    すべての野鳥の基本情報（名前、説明、画像パスなど）がリスト形式で保存されている、図鑑の「本体」です。
2.  **`/public/images/`**: 
    図鑑で表示されるすべての画像が格納されているフォルダです。
3.  **`/src/data/birds_md/`**: 
    （オプション）特定の鳥のページをブログのようなリッチなコンテンツにしたい場合に、Markdown形式の記事ファイルを置くフォルダです。

---

## 1. 新しい野鳥を追加する方法

新しい鳥を追加するには、「① データ（JSON）の追加」と「② 画像の追加」の2つのステップが必要です。

### 手順1-1: `birds.json` に鳥のデータを追加する

1.  `src/data/birds.json` ファイルを開きます。
2.  既存の鳥のデータブロック（`{` から `}` まで）をどれか一つ、丸ごとコピーします。
3.  コピーしたブロックを、リストの最後（最後の `}` の手前）に貼り付けます。**この時、直前の `}` の後ろにカンマ `,` を必ず追加してください。**
4.  貼り付けたブロックの内容を、新しい鳥の情報に書き換えます。

**【重要】 各項目の意味**
- `id`: **（必須）** 他の鳥と重複しない、ユニークな**半角英数字**のID。これがページのURLになります。（例: `"shimaenaga"`）
- `name`: 鳥の和名。（例: `"シマエナガ"`）
- `scientificName`: 鳥の学名。（例: `"Aegithalos caudatus"`）
- `category`: カテゴリー名。既存のカテゴリーに合わせるか、新しいカテゴリー名を付けられます。
- `description`: 図鑑の標準ページで表示される、鳥の簡単な説明文。
- `mainImage`: 一覧ページや詳細ページのメインで表示される画像のパス。`/images/【idと同じ名前】/main.jpg` のようにするのがおすすめです。
- `galleryImages`: 詳細ページ下部のギャラリーに表示される画像のリスト。

**▼ `birds.json` の編集例（シマエナガを追加する場合）**
```json
[
  {
    "id": "hayabusa",
    "name": "ハヤブサ",
    // ... (ハヤブサのデータ) ...
  },
  { // ← 新しく追加したブロック
    "id": "shimaenaga",
    "name": "シマエナガ",
    "scientificName": "Aegithalos caudatus",
    "category": "小さな鳥",
    "description": "白くて丸い体が特徴の、非常に可愛らしい小鳥。「雪の妖精」とも呼ばれる。",
    "mainImage": "/images/shimaenaga/main.jpg",
    "galleryImages": [
      "/images/shimaenaga/01.jpg",
      "/images/shimaenaga/02.jpg"
    ]
  }
]
```

### 手順1-2: 画像ファイルを追加する

1.  `public/images/` フォルダの中に、先ほど `id` で指定した名前の新しいフォルダを作成します。（例: `shimaenaga` フォルダ）
2.  作成したフォルダの中に、`birds.json` で指定したファイル名の画像（`main.jpg`, `01.jpg` など）を置きます。

これだけで、サイトに新しい鳥が自動的に追加されます。

---

## 2. 既存の野鳥の情報を編集する方法

- **説明文やカテゴリーを変更したい場合:**
  `src/data/birds.json` を開き、該当する鳥の `description` や `category` の内容を書き換えるだけです。

- **画像を差し替えたい場合:**
  `public/images/` の中の該当する鳥のフォルダを開き、同じファイル名で画像を上書き保存します。

---

## 3. 特定の野鳥を「ブログ形式」のページにする方法

通常は `birds.json` の情報が表示されますが、特定の鳥だけ、もっと詳しく解説したブログのようなページに差し替えることができます。

1.  `src/data/birds_md/` フォルダの中に、`【鳥のid】.md` という名前で新しいファイルを作成します。（例: `mejiro.md`）
2.  作成したファイルに、以下の形式で内容を記述します。

**▼ `mejiro.md` の記述例**
```markdown
---
title: '特集：春を告げる小さな宝石、メジロ'
author: 'サイト管理人'
date: '2025-07-26'
---

## メジロってどんな鳥？

ここに自由に文章を書くことができます。
**太字**にしたり、リストを使ったりもできます。

- 見出し
- 箇条書き

> 引用符も使えます。

このように、Markdown記法で自由にコンテンツを作成してください。
```

このファイルを作成するだけで、`/zukan/mejiro` にアクセスした際、自動的にこちらのブログ形式のページが表示されるようになります。

---

## 4. 野鳥を削除する方法

1.  `src/data/birds.json` を開き、削除したい鳥のデータブロック（`{` から `}` までと、その前後のカンマ）を丸ごと削除します。
2.  `public/images/` フォルダから、該当する鳥の画像フォルダを削除します。
3.  （もしあれば）`src/data/birds_md/` フォルダから、該当する鳥の `.md` ファイルを削除します。

